- finish session management --> storing session info on backend and setting/sending cookies
- when generating the friend activities, you replace the last friends activity witht the current, so you need to append to the array and after looping over all the friends, then and only then should you sort the array by date